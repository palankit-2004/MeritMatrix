[build]
  # This command grants permission to vite before running your original build script
  command = "chmod +x ./node_modules/.bin/vite && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200